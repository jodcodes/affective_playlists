end to end test
    test temperament_analyzer 
    meta enrich
    plsort
consolidate docs: one project summary, quickstart and readme; remove old docs
one license, gitignore, activate sh
use whitelisting as config for each subrepo individually
get backwards compatibility and orphaned code removed